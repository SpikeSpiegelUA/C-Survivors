#include "Bullet.h"

void AddBullet(Bullet* gameState, float x, float y, float dx) {
    int foundIndex = -1;
    //for (int index = 0; index < 1000; index++) {
    //    if (gameState->bullets[index] == NULL)
    //        foundIndex = index;
    //}
}

void RemoveBullet(int bulletsArrayIndex) {

}